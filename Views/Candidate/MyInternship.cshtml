@model List<MyInternshipViewModel>

@{
    Layout = "~/Views/Shared/_DashboardLayout.cshtml";

    ViewData["Title"] = "My Internship";
    ViewData["Panel"] = "Candidate";
    ViewData["UserName"] = "Candidate";
}

<div class="container-fluid">

  


    <!-- MASTER TABLE CARD -->
    <div class="master-table-container">

        


        <!-- TABLE -->
        <div class="table-responsive">
              <!-- PAGE HEADING -->
    <div class="mb-4">

        <h1 class="mb-2">
            My Internship
        </h1>

        <p class="mb-0 text-muted">
            HN236383503368076
        </p>

    </div>
            <table id="candidateTable"
                   class="master-data-table">

                <thead>

                    <tr>

                        <th>Sr. No</th>

                        <th>Organization Name</th>

                        <th>Position</th>

                        <th>Location</th>

                        <th>Internship / Fellowship Type</th>

                        <th>Facilities</th>

                        <th>Apply Status</th>

                    </tr>

                </thead>


              <tbody>

    @if (Model != null && Model.Count > 0)
    {
        int srNo = 1;

        foreach (var item in Model)
        {
            <tr>

                <!-- SR NO -->
                <td class="master-id">
                    @srNo
                </td>

                <!-- ORGANIZATION NAME -->
                <td>
                    @item.OrganizationName
                </td>

                <!-- POSITION -->
                <td>
                    @item.Position
                </td>

                <!-- LOCATION -->
                <td>
                    @item.Location
                </td>

                <!-- INTERNSHIP / FELLOWSHIP TYPE -->
                <td>
                    @item.InternshipFellowshipType
                </td>

                <!-- FACILITIES -->
                <td>
                    @item.Facilities
                </td>

                <!-- APPLY STATUS -->
                <td>

                    @if (!string.IsNullOrWhiteSpace(item.ApplyStatus))
                    {
                        <span class="master-status">
                            @item.ApplyStatus
                        </span>
                    }

                </td>

            </tr>

            srNo++;
        }
    }

</tbody>

            </table>

        </div>


        <!-- PAGE INFO -->
        <div id="candidatePageInfo"
             class="master-page-info text-center d-none">
        </div>

    </div>

</div>